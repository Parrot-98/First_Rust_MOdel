[package]
name = "First_Model"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "~0.20", features = ["std", "tui", "train", "vision", "wgpu", "fusion"], default-features = false }
